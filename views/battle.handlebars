<ul id="navbar">
    <li><a href="?">Home</a></li>
    <li><a href="?">About Us</a></li>
    <li><a href="?">Logout</a></li>
</ul>
{{character1.name}}
<div id="battle-container">
    <h1>Battle</h1>
    <div id="mb-health-container">
        <div id="message-board">
            <h2 id="message-board-header">
                Message Board
            </h2>
            <p id="battle-message"></p>
        </div>
        <div id="health">
            <h2>My Health = <span id="myHealth" class="span" data-characte={{character1}}>{{character1.hp}}</span></h2>
            <h2>Foe's Health = <span id="foeHealth" class="span">{{character2.hp}}</span></h2>
        </div>
    </div>
    <br>
    <button id="physical-button" class = "attack" data-type="physAtk">Physical Attack</button>
    <button id="magical-button" class = "attack" data-type="magAtk">Magical Attack</button>
</div>

<div id="game-over-modal">
    <div id="modal-content">
        <span id="close">&times;</span>
        <h2>Game Over</h2>
        <h3>You {{result}}.</h3>
        <p>Helpi nenii responde il pro. Sekve hebrea nul o, be des tria hebrea depost, do nun centi glota
            senobjekta. Ar
            sube kialo esperantigo kie, li unua tioma eca. Substantiva postpostmorgaŭ ien om, jo ind duono spite
            proksimumeco, peto kaŭze respondvorto de esk. Cit us viro landonomo, hav dz deci hodiaŭa solinfano.</p>
    </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

{{!-- <script >
  function turn(atkType) {
    console.log("Kachow");
    let coinFlip = Math.floor(Math.random() * 2);
    if (coinFlip === 0) {
      character1.attack(character2, atkType);
      //display the messages set to console.log in the atk function in the message space.
      if (character2.hp === 0) {
        endGame();
      }
      character2.attack(character1, atkType);
      //display the messages set to console.log in the atk function in the message space.
      if (character1.hp === 0) {
        endGame();
      }
    } else {
      character2.attack(character1, atkType);
      //display the messages set to console.log in the atk function in the message space.
      if (character1.hp === 0) {
        endGame();
      }
      character1.attack(character2, atkType);
      //display the messages set to console.log in the atk function in the message space.
      if (character2.hp === 0) {
        endGame();
      }
    }
    return;
  }
  $("#physical-button").on("click", function(){turn("physAtk")})
</script> --}}
<script src="/js/battle.js"></script>
